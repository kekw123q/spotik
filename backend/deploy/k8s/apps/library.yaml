apiVersion: apps/v1
kind: Deployment
metadata:
  name: library
spec:
  replicas: 1
  selector:
    matchLabels:
      app: library
  template:
    metadata:
      labels:
        app: library
    spec:
      containers:
        - name: app
          image: spotik/library:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: config-volume
              mountPath: /app/configs
              readOnly: true
          env:
            - name: APP_LIBRARY_DB_DSN
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: library-dsn
      volumes:
        - name: config-volume
          configMap:
            name: app-config
---
apiVersion: v1
kind: Service
metadata:
  name: library
spec:
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: library